# Plan: initial project scaffold

**Assisted-by: Hermes Agent · GLM 5.3 Flash <hermes-ollamacloud-glm53flash@local>** (generated by `agent-detect trailer assisted-by`, not cached)
**Provenance:** see [1788701508000-initial-scaffold.prompts.md](./1788701508000-initial-scaffold.prompts.md)

## Goal

Scaffold `bevry-vibes/automate-apple-shortcuts-automations` per bevry-vibes/skills: reciprocity gate via `agent-detect` (passed: Hermes Agent + Ollama Cloud + GLM 5.3 Flash = reciprocal), bevry/base config files, RPL-1.5 LICENSE.md, `.plans/` convention adopted, DESIGN.md (design learnings from the live automation session), README.md (end-user guide for Hermes + GLM 5.3 Flash), AGENTS.md pointer file, conventional commits with agent-detect co-author trailers, push to `main`.

## Steps

1. ✅ Verify repo on GitHub (exists, private, empty, description set), gh auth, git identity (`Benjamin Lupton <b@lupton.cc>` = license default)
2. ✅ agent-detect: `identify` → reciprocal ✓; trailers: `Assisted-by: Hermes Agent · GLM 5.3 Flash <hermes-ollamacloud-glm53flash@local>`
3. ✅ Repo init at `~/Projects/vibes/automate-apple-shortcuts-automations` (branch `main`, remote `origin` git@github.com:bevry-vibes/automate-apple-shortcuts-automations.git)
4. ✅ bevry/base configs (`.editorconfig` 432B, `.gitattributes` 244B, `.gitignore` 629B) + custom banners; LICENSE.md (RPL-1.5) fetched
5. Write DESIGN.md (learnings), README.md (usage), AGENTS.md (remote-skill pointers + local tweaks), .plans/ companion
6. Commit per commits.md (conventional, ≤50-char titles, one logical change each, co-author trailer via agent-detect)
7. Push main; gh repo edit --add-topic, --enable-issues? (ask user); verify pushed state

## Non-goals

- No runtime code: this repo is documentation/plans only for now (the automation was performed live via Hermes GUI automation; future work may extract reusable scripts)
- No CI setup (empty docs repo; revisit when code lands)

## Open questions

- Repo visibility: currently **private** — publish as public? (agent-detect reciprocity passed, so per policy any permitted agent can work on it either way)
- Topics/description tweaks wanted beyond the set description?